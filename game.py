from board import Board
from text import *

greetPlayer()

def gameLoop():
  board = Board(2, 2, 2)

  #greetPlayer
  #explainRules
  #createBoard(size, difficulty)
  #displayBoard
    #for now just draw it in terminal